def linearSearch(arr, targetVal):
    for i in range(len(arr)):
        if arr[i] == targetVal:
            return targetVal
            
arr = [3, 7, 2, 9, 5]
targetVal = 9
print(linearSearch(arr, targetVal))
